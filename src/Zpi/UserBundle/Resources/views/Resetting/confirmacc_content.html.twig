{% form_theme form theme %}
<form novalidate="novalidate" action="{{ path('confirmacc', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.confirmacc.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>
